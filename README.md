# Information Retrieval

## Week 2: Boolean retrieval

```bash
python3 ir_system.py
```

## Week 3: TF-IDF document similarity

```bash
python3 -m pip install -r requirements.txt
python3 week3.py
```

`week3.py` creates its eight sample source documents when they are absent and
writes CSV results plus a similarity heatmap to `results/`.
